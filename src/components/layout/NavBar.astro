---
import DashboardHeader from "./DashboardHeader.astro";
import NavHeader from "./NavHeader.astro";

interface Props {
    email?: string;
    userName?: string;
}

const { email, userName } = Astro.props;
---

{!userName ? <NavHeader /> : <DashboardHeader email={email} userName={userName} />}
